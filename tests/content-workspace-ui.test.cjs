'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..');const read=(p)=>fs.readFileSync(path.join(root,p),'utf8');
test('Create is enabled and loads every Content Studio application layer',()=>{const navigation=require('../src/renderer/navigation-model.js');assert.equal(navigation.getRoute('create').enabled,true);const loader=read('src/renderer/navigation-model.js');for(const file of ['content-studio-ui.js','content-writing-ui.js','content-storyboard-ui.js','content-workspace-ui.js'])assert.match(loader,new RegExp(file.replace('.','\\.')));const bootstrap=read('src/main/application-bootstrap.cjs');assert.match(bootstrap,/content-workspace-bootstrap/);});
test('preload exposes named workspace actions without a generic IPC or filesystem surface',()=>{const source=read('src/preload/preload-bridge.cjs');for(const method of ['getContentWorkspace','editContentTimeline','saveTimedText','importContentAudio','saveContentAudio','saveContentCover','exportContentCover','saveExportVariant','validateContentExport','renderContentExport'])assert.match(source,new RegExp(method));assert.doesNotMatch(source,/sendSync|exposeInMainWorld\([^,]+,\s*ipcRenderer/);});
test('creator workspace provides the complete bounded creator controls',()=>{const source=read('src/renderer/content-workspace-ui.js');for(const phrase of ['Build from storyboard','Editable visual timeline','Trim','Split at playhead','Replace clip','Undo','Playhead / scrub','Timed text editor','Duplicate','Import local music or voiceover','Keep original audio','Cover workspace','Export PNG','Validate export','Choose destination & render'])assert.match(source,new RegExp(phrase,'i'));assert.match(source,/textContent/);assert.doesNotMatch(source,/shell|child_process|fetch\(|https?:\/\//);});
test('trusted main workspace performs real local cover and video exports',()=>{const source=read('src/main/content-workspace-bootstrap.cjs');for(const token of ['showSaveDialog','CoverExporter','RenderEngine','prepareVariantRender','createExportRecord','withTimedText','withAudio'])assert.match(source,new RegExp(token));assert.match(source,/content-audio/);assert.match(source,/COPYFILE_EXCL/);assert.doesNotMatch(source,/shell:true|openExternal|https?:\/\//);});
